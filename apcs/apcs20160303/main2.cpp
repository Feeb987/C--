#include<bits/stdc++.h>

using namespace std;

int main(){

    int r, c, m, b;
    cin >> r >> c >> m;
    b = max(r, c);
    int arr[b][b];
    for(int i = 0;i < r;i++){
        for(int l = 0;l < c;l++){
            cin >> arr[i][l];
        }
    }
    for(int i = 0;i < m;i++){
        int in;
        cin >> in;
        if(in){
````````````
        }
        if(!in){
            
        }
    }

    return 0;

}