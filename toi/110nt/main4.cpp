#include<bits/stdc++.h>
using namespace std;
long long b = LLONG_MIN;
int n, k;

int dfs(int a, vector<int> s, int cnt){
    if(cnt < k){
        for(auto it:s){
            vector<int> v(s.begin(), v.end());
            int add = v.begin();
            v.erase(add+i, add+i+1)
        }
    }
}

int main(){

    cin >> n >> k;
    vector<int> s(n);
    for(auto &it:s) cin >> it;

    return 0;
    
}